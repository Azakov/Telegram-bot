import info.mukel.telegrambot4s.models.User

import scala.util.Try
import scala.util.parsing.combinator._
val a = Map("AL" -> "Alabama","lel" -> "moew")
a.map(u => s"${u._1.mkString("")} ${u._2}").mkString("\n")

//case class kek() extends Exept

